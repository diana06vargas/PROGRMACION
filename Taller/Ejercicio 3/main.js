'use strict';
//3. Realizar la suma de N número ingresados por el usuario.

const arreglo = [n];

let n = parseFloat (prompt('Dame la cantidad de números que deses sumar'));

const sumar = (arreglo) => {

}